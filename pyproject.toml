[project]
name = "twitter-local"
version = "0.1.0"
description = "IndexedDB-to-Markdown exporter with platform adapters for social sites."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "playwright>=1.58.0",
]
